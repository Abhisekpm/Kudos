DATABASE_URL="file:./dev.db"

# Gamemaster passcode — full write access (check off habits, manage habits,
# nominations, chores, deductions, lock weeks).
GM_PASSCODE=""

# Shared family passcode — read-only Play view.
PLAY_PASSCODE=""

# Random 32+ byte secret used to encrypt session cookies (iron-session).
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=""
